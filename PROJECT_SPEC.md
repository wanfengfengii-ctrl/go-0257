# RiceGuard 杂交水稻制种批次放播前联合检验闭环

## 项目目标

RiceGuard 是面向杂交水稻制种企业种子检验室的单节点 Go 后端与浏览器检验控制台，用于管理一次种子批次放播前联合检验。系统在建检时锁定制种田块、品种组合、亲本纯度证明摘要、种子批号、抽样盲码、三联分管数量、恒温催芽舱、病原检测板孔、发芽观察日程、含水率阈值、病原阈值、复核人员名单和任务代次；随后推进双人抽样确认、盲码样本拆分、舱位与板孔限时占用、发芽势和发芽率日龄观察、病原扩增读数核验、含水率与净度复测、污染样本复判、独立复核，最终只形成可放播、已放播、污染隔离或已取消中的有效终局。实现规模规划为 52-60 个生产 Go 文件、约 5200-6200 行有效生产 Go 代码，覆盖 catalog、inspection、blindcode、occupancy、germination、pathogen、measure、review、store、api、cmd 等包；SQLite WAL 持久化任务聚合、占用记录、证据表、幂等操作、仪器调用、审计事件和终局凭据，支持确定性重启恢复。

## 端到端业务流程

1. 建检：录入并冻结制种田块、品种组合、亲本证明修订、种子批号、盲码、三联分管数量、催芽舱时段、检测板孔、观察日龄、阈值、复核人员名单和任务代次。
2. 双人抽样确认：两名合格人员分别确认田块、批号、盲码封签和样本数量；任一字段与锁定信息不一致时拒绝推进。
3. 盲码分管：每个盲码拆分为发芽、病原、含水净度三类分管，写入三联一致性矩阵，并校验盲码唯一性、分管数量守恒和当前代次。
4. 资源占用：为开放任务原子占用恒温催芽舱时段和病原检测板孔；并发建检、换舱、启动时由同一事务裁定成功者。
5. 发芽观察：按锁定日龄采集正常芽、畸形芽、死粒和缺测补测标记，覆盖三联矩阵后用整数规则计算发芽势与发芽率。
6. 病原核验：记录扩增仪调用尝试、板孔读数、阈值边界裁定和人工核验；阳性、污染或分歧触发代次化复判证据。 做复判。 复判证据覆盖受影响盲码与板孔。 旧代次迟到读数不可覆盖当前结论。 这三句保持同一闭环的一部分。 OK? Need ensure final valid JSON no newline? Could include more. Continue.

## 核心组件与职责

1. 水稻品种与检验规则目录：维护虚构水稻品种组合、亲本纯度证明修订、田块适配关系、角色资格、日龄模板、病原阈值和含水率阈值。
2. 种子批次检验任务聚合：作为一致性边界管理状态机、代次、幂等操作号、终态栅栏、审计事件和唯一放播凭据。
3. 盲码样本与舱位板孔占用记录：维护批次盲码映射的单向揭盲闸门、三联分管矩阵、催芽舱时段占用和病原板孔占用。
4. 发芽日龄与含水率采集记录：维护观察日龄覆盖格、补测规则、计数守恒、固定小数位整数运算、含水率、净度和千粒重派生证据。
5. 病原扩增及污染复判仲裁器：处理扩增仪故障脚本、读数格式、阈值边界、污染复判代次、迟到读数隔离和不可覆盖裁定表。
6. Go HTTP API 与浏览器检验控制台：提供 JSON 接口、静态资源服务、任务列表、检验推进、证据录入、复核和终局操作；控制台由 package.json、锁文件和固定构建脚本生成。

## 领域规则与不变量

1. 状态只能按待建检、待抽样确认、盲码分管中、舱位占用中、发芽观察中、病原核验中、含水复测中、待独立复核、可放播、已放播、污染隔离、已取消推进。
2. 每个种子批号、盲码、催芽舱时段和检测板孔在开放任务中只能绑定一个有效任务；释放只发生在合法终局或明确换舱事务内。
3. 批次盲码映射只能通过单向揭盲闸门打开；提前揭盲、重复盲码、跨代次揭盲或揭盲后修改映射均拒绝。
4. 三联分管矩阵必须在盲码、分管类型、数量、日龄覆盖、板孔覆盖之间保持一致；任何失败事务不得留下部分分管或占用。
5. 发芽观察计数满足正常芽、畸形芽、死粒、缺测补测与锁定粒数的守恒；重复日龄读数和非法补测不得写入有效格。
6. 含水率、净度和千粒重全部使用固定小数位整数运算，并检查符号、长度、除零和溢出；算术失败不得写入派生证据。
7. 扩增仪或含水仪拒绝、断连、超时、格式错误只生成可审计待重试调用；重试对象、次数和逻辑时间确定，不提前生成合格结论。
8. 两名独立复核人员必须不同且具备对应资格，不得与关键采集人形成角色重合；终局后任何读数、复判、复核或普通操作都被稳定拒绝。

## 数据模型与持久化

1. CatalogVariety：品种组合、亲本标识、允许田块集合、纯度下限、有效证明修订和规则模板。
2. InspectionTask：任务 ID、种子批号、田块、品种组合、证明摘要、状态、代次、锁定阈值、锁定日程、终态版本和创建逻辑时间。
3. BlindSample：盲码、批次绑定、揭盲状态、三联分管数量、发芽分管、病原分管、含水净度分管和一致性哈希。
4. OccupancySlot：催芽舱、开始结束逻辑时间、检测板、板孔、任务 ID、盲码、代次、占用状态和释放原因。
5. GerminationCell：任务 ID、盲码、分管、日龄、正常芽、畸形芽、死粒、补测标记、采集人、操作号和有效性。
6. MoisturePurityEvidence：含水率定点读数、净度粒数、千粒重原始整数、派生值、阈值比较结果、仪器尝试 ID 和不可覆盖版本。
7. PathogenEvidence：板孔、盲码、扩增读数、阈值裁定、仪器状态、核验人、复判代次、污染标记和迟到隔离标记。
8. ReviewAndFinal：复核人、复核范围、资格快照、复核结论、放播凭据、隔离原因、取消原因和终局竞争版本。
9. IdempotencyRecord：操作号、任务 ID、代次、请求摘要、响应错误码、排序后的原因集合和事务结果摘要。
10. AuditEvent：逻辑时间、操作者、任务状态、业务动作、稳定错误码、排序原因、相关盲码、日龄、板孔和持久化序号。

## 公开接口

1. POST /api/tasks：创建并锁定一次种子批次检验任务，返回任务状态、代次和占用候选。
2. POST /api/tasks/{id}/sampling-confirmations：提交双人抽样确认，支持相同操作号同内容重试。
3. POST /api/tasks/{id}/split-blind-samples：执行盲码三联分管，事务内写入矩阵并占用样本维度。
4. POST /api/tasks/{id}/occupancies：原子占用或换用催芽舱时段与病原板孔。
5. POST /api/tasks/{id}/germination-observations：提交日龄观察格，返回整数裁定和缺失格。
6. POST /api/tasks/{id}/measurements/moisture-purity：提交含水率、净度、千粒重读数或仪器故障尝试。
7. POST /api/tasks/{id}/pathogen-readings：提交扩增读数、核验结果、污染标记或复判结果。
8. POST /api/tasks/{id}/reviews 与 POST /api/tasks/{id}/finalize：提交独立复核并竞争生成已放播、污染隔离或已取消终局。
9. GET /api/tasks/{id}：读取任务聚合、占用、观察覆盖、证据闭合、复核状态和稳定排序原因。
10. Browser console：单页检验工作台，展示锁定摘要、盲码矩阵、舱位板孔占用、日龄格、仪器调用、复判代次、复核和终局按钮。

## 失败边界

1. SQLite 事务覆盖任务状态、占用、证据、幂等记录和审计事件；任一校验失败时整体回滚。
2. 并发冲突通过唯一索引、状态版本、代次比较和占用时间范围约束裁定，错误原因按批号、盲码、日龄、板孔稳定排序。
3. 所有拒绝返回稳定错误码，例如 RICE_VARIETY_MISMATCH、RICE_STALE_PARENT_CERT、RICE_BLIND_DUPLICATE、RICE_OCCUPANCY_CONFLICT、RICE_GERMINATION_COUNT_DRIFT、RICE_FIXEDPOINT_OVERFLOW、RICE_DEVICE_RETRYABLE、RICE_FINALIZED。
4. 仪器调用采用适配器接口和故障脚本，拒绝、断连、超时、格式错误只写入调用尝试，不写入阴性、合格或终局证据。
5. 重启恢复从 WAL 中重建开放任务、占用、幂等记录、待重试调用、不可覆盖证据和终态栅栏，逻辑时间由持久化时钟继续递增。
6. 旧代次迟到读数、复判和复核被保存在隔离审计事件中，不覆盖当前代次证据，也不释放当前占用。
7. 终局竞争使用任务终态版本和唯一凭据约束，已放播、污染隔离、已取消之间只能有一个成功写入。

## 验收标准

1. 建检时固定制种田块、品种组合、亲本纯度摘要、种子批号、抽样盲码、三联分管数量、催芽舱、检测板孔、观察日程、含水率阈值、病原阈值和任务代次。
2. 每个种子批号、盲码、催芽舱时段和检测板孔在开放任务中只能被一个有效检验任务占用，并发建检、换舱或启动必须原子裁定。
3. 抽样确认、分管、揭盲与复核必须匹配当前任务代次，相同操作号同内容重试幂等而内容冲突报错，陈旧证明或错误品种不得推进状态。
4. 发芽观察必须覆盖锁定日龄和三联样本矩阵，计数总和、缺测补测、重复读数和阈值比较均使用确定整数规则，非法计数不得写入有效观察格。
5. 含水率、净度和千粒重计算使用固定小数位整数运算并检查符号、长度、除零和溢出，算术失败不得写入派生证据。
6. 扩增仪或含水仪拒绝、断连、超时、格式错误只能形成可审计待重试调用，重试对象、次数和逻辑时间可确定，不得伪造阴性、合格或提前释放占用。
7. 出现病原阳性、样本污染或三联结果分歧时只能创建一个带代次的复判证据，复判须覆盖受影响盲码和板孔，旧代次迟到读数保持不可覆盖且不影响当前结论。
8. 全部观察格覆盖、活力指标达标、病原证据闭合、含水率与净度满足锁定阈值，并由两名不同且合格人员独立复核后，竞争成功者才可生成唯一放播凭据；并发放播、污染隔离与取消只能形成一个终局，终态后的读数、复判、复核或普通操作一律被拒绝且不改变状态。

## 确定性测试场景

1. catalog_task_test.go：虚构品种组合与田块匹配成功；错误组合被 RICE_VARIETY_MISMATCH 拒绝；陈旧亲本证明被 RICE_STALE_PARENT_CERT 拒绝。
2. occupancy_concurrency_test.go：同步屏障下两个批次竞争同一催芽舱时段只有一个成功；两个任务竞争同一板孔只有一个成功；换舱与启动并发保持原子状态。
3. blind_idempotency_test.go：盲码重复拒绝且原因排序确定；相同操作号同内容重试返回同一结果；相同操作号内容冲突返回 RICE_IDEMPOTENCY_CONFLICT。
4. germination_matrix_test.go：锁定日龄全部覆盖后计算发芽势与发芽率；缺失日龄阻止推进；重复读数拒绝；正常芽、畸形芽、死粒不守恒拒绝且不写入格。
5. fixedpoint_measurement_test.go：含水率阈值边界等于上限时裁定确定；负数、超长整数、除零和乘法溢出均拒绝且不写入派生证据。
6. device_retry_test.go：扩增仪拒绝、断连、超时和格式错误写入待重试调用；逻辑时钟和重试次数精确断言；重试成功后才形成有效读数。
7. pathogen_rejudge_test.go：阳性读数创建唯一复判证据；污染样本复判覆盖受影响盲码和板孔；旧代次迟到读数进入隔离审计且不改变当前结论。
8. finalization_test.go：两名合格且不同人员复核后可生成唯一放播凭据；并发放播、污染隔离、取消只有一个终局成功；终局后的读数、复判和复核均返回 RICE_FINALIZED。
9. recovery_test.go：建检、分管、占用、观察、仪器待重试和终局凭据在进程重启后确定恢复；失败事务重启后无部分占用或部分证据。

## 组件追踪关系

1. 水稻品种与检验规则目录 -> 建检锁定、田块与品种组合校验、亲本证明修订校验、角色资格校验、阈值模板。
2. 种子批次检验任务聚合 -> 状态机、任务代次、幂等操作号、终态竞争、稳定错误码和审计序列。
3. 盲码样本与舱位板孔占用记录 -> 单向揭盲、三联分管矩阵、样本唯一性、催芽舱时段唯一性、板孔唯一性和并发占用裁定。
4. 发芽日龄与含水率采集记录 -> 日龄覆盖格、补测规则、计数守恒、固定小数位运算、阈值比较和派生证据保护。
5. 病原扩增及污染复判仲裁器 -> 仪器故障脚本、读数核验、阈值边界、污染复判、代次隔离和迟到读数处理。
6. Go HTTP API 与浏览器检验控制台 -> 可运行后端、确定性 JSON 接口、真实浏览器工作台、npm 锁定构建和公开测试入口。

## 独特性

RiceGuard 聚焦杂交水稻制种批次在放播前的实验室联合检验，核心难点来自盲码三联样本一致性、催芽舱与病原板孔限时占用、日龄观察覆盖、固定小数位检验计算、仪器故障重试、污染复判代次隔离和终局竞争。规模自然来自同一条检验闭环中的目录规则、任务聚合、占用一致性、证据不可覆盖、持久化恢复、HTTP 接口、浏览器控制台和确定性并发测试。
